# Limitations

Known current limitations:

- Retrieval labels are still development labels.
- Corpus extraction may contain PDF artifacts.
- CI does not require a local model server.
- Judge validation has not started.
